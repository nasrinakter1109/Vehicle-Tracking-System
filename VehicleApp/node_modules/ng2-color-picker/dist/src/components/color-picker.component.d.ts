import { OnInit } from '@angular/core';
import { NgModel, ControlValueAccessor } from '@angular/forms';
import { ColorPickerConfiguration } from '../models';
import { IColorPickerConfiguration } from '../interfaces';
export declare class ColorPickerComponent implements ControlValueAccessor, OnInit {
    cd: NgModel;
    onChange: any;
    onTouched: any;
    config: ColorPickerConfiguration;
    pickerConfig: IColorPickerConfiguration;
    availableColors: string[];
    constructor(cd: NgModel);
    ngOnInit(): void;
    writeValue(value: any): void;
    registerOnChange(fn: (_: any) => {}): void;
    registerOnTouched(fn: (_: any) => {}): void;
    setColor(color: string): void;
    private _processOptions(opts);
}
